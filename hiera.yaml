---
:backends:
  - json
  - yaml
:hierarchy:
  - cloud-init
  - %{fqdn}
  - %{clientcert}
  - %{environment}
  - global
  - default

:json:
  :datadir: /etc/puppet/hieradata
:yaml:
  :datadir: /etc/puppet/hieradata
