---
:backends:
  - json
  - yaml
:hierarchy:
  - cloud-init
  - %{fqdn}
  - %{hostname}
  - %{clientcert}
  - %{environment}
  - global
  - default

:json:
  :datadir: /etc/puppet/hieradata
:yaml:
  :datadir: /etc/puppet/hieradata
