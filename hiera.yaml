---
:backends:
  - json
  - yaml
:hierarchy:
  - cloud-init
  - user-data
  - %{fqdn}
  - %{hostname}
  - %{environment}
  - global
  - default

:json:
  :datadir: /etc/puppet/hieradata
:yaml:
  :datadir: /etc/puppet/hieradata
